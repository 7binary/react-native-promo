import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    marginBottom: 5,
    borderRadius: 10,
  },
  status: {
    textAlign: 'center',
    fontWeight: 'bold',
    paddingVertical: 2,
    paddingHorizontal: 10,
    fontSize: 16,
    borderRadius: 5,
    overflow: 'hidden',
  },
});
